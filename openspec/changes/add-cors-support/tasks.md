## 1. HTTP layer

- [x] 1.1 Register `fastapi.middleware.cors.CORSMiddleware` on the app in
  `api/main.py`, allowing origins `http://localhost:5173` and
  `http://127.0.0.1:5173`, methods `GET` only, all headers

## 2. Docs

- [ ] 2.1 Note browser/CORS support and the allowed local origins in
  `api/README.md`
